# Task 4: Update ProjectRegistrationService Payload

**Status**: todo
**Phase**: 3 — Registration + Generator Updates

## Context

**Spec**: `.vanguard/specs/dynamic-init-options.md` (FR-007)
**Plan**: `.vanguard/plans/dynamic-init-options.md` (Component 5)
**Feature**: Dynamic Init Options

## Objective

Modify the `ProjectRegistrationPayload` interface to make `vanguardConfig` optional and add the new `selections` field.

## Acceptance Criteria

- [ ] `vanguardConfig` field changed from required to optional (`vanguardConfig?: Record<string, unknown>`)
- [ ] New `selections?: Record<string, string>` field added to `ProjectRegistrationPayload`
- [ ] `register()` method continues to work unchanged (JSON.stringify handles optional fields)
- [ ] No changes to `ProjectRegistrationResult`
- [ ] Existing callers that pass `vanguardConfig` still compile (backward compat)
- [ ] Lint passes

## Implementation Notes

### Files to Create/Modify

| File | Action | Description |
|------|--------|-------------|
| `src/application/services/project-registration.service.ts` | Modify | Update `ProjectRegistrationPayload` interface |

### Architecture Guidance

**Layer**: Application (Service)

**Change is minimal** — only the interface definition changes:

```diff
 export interface ProjectRegistrationPayload {
   name: string
   type: 'greenfield' | 'brownfield'
   track: 'solo' | 'team' | 'enterprise'
   projectPath: string
   gitRemoteUrl?: string | undefined
   defaultBranch?: string | undefined
-  vanguardConfig: Record<string, unknown>
+  vanguardConfig?: Record<string, unknown>
+  selections?: Record<string, string> | undefined
 }
```

The `register()` method body does not change. `JSON.stringify` already handles optional/undefined fields by omitting them.

## Dependencies

- [ ] Depends on: None (can be done independently)
- [ ] Blocks: Task 6

## Testing Requirements

- [ ] No new tests required — existing functionality unchanged. Verified by existing tests (if any) continuing to pass.

---
_Generated by Vanguard for vanguard-cli_
