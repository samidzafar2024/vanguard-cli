# Task 8: Remove Local Registries (Follow-up PR)

**Status**: complete
**Phase**: 5 — Cleanup (separate PR)

## Context

**Spec**: `.vanguard/specs/dynamic-init-options.md` (FR-X05 — deferred)
**Plan**: `.vanguard/plans/dynamic-init-options.md` (Phase 5)
**Feature**: Dynamic Init Options

## Objective

Remove the local option registry files that are no longer used after the dynamic init flow is live and validated.

## Acceptance Criteria

- [x] `src/presentation/data/stacks/index.ts` deleted (1,077 lines)
- [x] `src/presentation/data/architectures/index.ts` deleted (953 lines)
- [x] `src/presentation/data/testing/index.ts` deleted (431 lines)
- [x] Any remaining imports of these files removed (verified: none existed)
- [x] Empty parent directories removed if no other files remain
- [x] No references to `stackRegistry`, `architectureRegistry`, `testingRegistry` remain in codebase
- [x] Old `SmartDefaults` interface removed (if still present) — N/A: `SmartDefaults` is the new interface in `dynamic-prompt-renderer.ts`, actively used
- [x] Old `getSmartDefaults()` function removed (if still present) — N/A: already removed, replaced by `mapDetectedToServerSlugs()`
- [x] Build succeeds
- [x] All tests pass
- [x] Lint passes

## Implementation Notes

### Files to Create/Modify

| File | Action | Description |
|------|--------|-------------|
| `src/presentation/data/stacks/index.ts` | Delete | 1,077-line stack registry |
| `src/presentation/data/architectures/index.ts` | Delete | 953-line architecture registry |
| `src/presentation/data/testing/index.ts` | Delete | 431-line testing framework registry |
| `src/presentation/data/stacks/` | Delete dir | If empty after file removal |
| `src/presentation/data/architectures/` | Delete dir | If empty after file removal |
| `src/presentation/data/testing/` | Delete dir | If empty after file removal |

### Architecture Guidance

**Layer**: Presentation (Data)

**Verification before deletion**: Run `grep -r "stackRegistry\|architectureRegistry\|testingRegistry" src/` to confirm no remaining references. The exploration during planning confirmed only `init.ts` imports these registries, and Task 6 removes those imports.

**This should be a separate PR** from the main dynamic init work. The main PR should be validated in production first. This cleanup PR is low-risk but should not be bundled with the feature work.

## Dependencies

- [ ] Depends on: Task 6 (init.ts no longer imports registries), Task 7 (tests pass without registries)
- [ ] Blocks: None

## Testing Requirements

- [ ] Build succeeds (`npm run build`)
- [ ] All existing tests pass (`npm run test`)
- [ ] Grep confirms zero references to deleted registries

---
_Generated by Vanguard for vanguard-cli_
