# Task: Update package.json for Public npm Registry

> Self-contained task with full context for updating package configuration.

## Context

**Spec**: `.vanguard/specs/npm-public-registry-publishing.md`
**Plan**: `.vanguard/plans/npm-public-registry-publishing.md`
**Epic**: Public npm Registry Publishing

## Objective

Update `package.json` to change the package scope from `@vanguard-data` to `@vanguard` and configure it for public npm registry publishing.

## Acceptance Criteria

- [ ] Package name changed from `@vanguard-data/vanguard-cli` to `vanguard-cli`
- [ ] `publishConfig.registry` field removed (defaults to registry.npmjs.org)
- [ ] `publishConfig.access` changed from `restricted` to `public`
- [ ] `npm pack --dry-run` shows correct package name `vanguard-vanguard-cli-*.tgz`
- [ ] Lint passes: `npm run lint`
- [ ] Typecheck passes: `npm run typecheck`

## Implementation Notes

### Files to Create/Modify

| File | Action | Description |
|------|--------|-------------|
| `package.json` | Modify | Update name and publishConfig fields |

### Current Configuration (Before)

```json
{
  "name": "@vanguard-data/vanguard-cli",
  "publishConfig": {
    "registry": "https://npm.pkg.github.com",
    "access": "restricted"
  }
}
```

### Target Configuration (After)

```json
{
  "name": "vanguard-cli",
  "publishConfig": {
    "access": "public"
  }
}
```

### Architecture Guidance

**Layer**: Infrastructure (package configuration)

**Pattern**: This is a configuration change, not a code change. No DDD patterns apply.

### Verification Steps

1. Run `npm pack --dry-run` to verify package name appears as `vanguard-vanguard-cli-0.1.0-alpha.1.tgz`
2. Run `npm run lint` to ensure JSON is properly formatted
3. Run `npm run typecheck` to ensure no TypeScript errors introduced

## Dependencies

- [ ] Depends on: None (first task)
- [ ] Blocks: task-002 (Rewrite release workflow)

## Testing Requirements

No automated tests required - this is configuration only.

### Manual Verification

```bash
# Verify package name in dry-run
npm pack --dry-run

# Expected output should include:
# npm notice name: vanguard-cli
# npm notice filename: vanguard-vanguard-cli-0.1.0-alpha.1.tgz
```

## Requirements Traceability

| Requirement | Status |
|-------------|--------|
| FR-001: Package published under `@vanguard` scope | Implemented by this task |
| FR-002: Support global installation | Already exists (bin field) |
| FR-003: `vanguard` command available | Already exists (bin field) |
| FR-009: Both `vanguard` and `vanguard-cli` commands | Already exists (bin field) |

---
_Generated by Vanguard for vanguard-cli_
